package com.vonagevoice.deprecated

/*
class VonageCallManager(private val voiceClient: VoiceClient) : CallManager {
    override fun answerCall(callId: String): Result<Unit> {
        TODO()
        /*return try {
            voiceClient.answerCall(callId)
            Result.success(Unit)
        } catch (e: Exception) {
            Result.failure(e)
        }*/
    }

    override fun rejectCall(callId: String): Result<Unit> {
        TODO()
        /*return try {
            voiceClient.rejectCall(callId)
            Result.success(Unit)
        } catch (e: Exception) {
            Result.failure(e)
        }*/
    }

    override fun hangup(callId: String): Result<Unit> {
        TODO()
       /* return try {
            voiceClient.hangupCall(callId)
            Result.success(Unit)
        } catch (e: Exception) {
            Result.failure(e)
        }*/
    }

    override fun mute(callId: String): Result<Unit> {
        TODO()
        /*return try {
            voiceClient.muteCall(callId)
            Result.success(Unit)
        } catch (e: Exception) {
            Result.failure(e)
        }*/
    }

    override fun unmute(callId: String): Result<Unit> {
        TODO()
        /*return try {
            voiceClient.unmuteCall(callId)
            Result.success(Unit)
        } catch (e: Exception) {
            Result.failure(e)
        }

         */
    }

    override fun reconnectCall(callId: String): Result<Unit> {
        TODO()
        /*return try {
            voiceClient.reconnectCall(callId)
            Result.success(Unit)
        } catch (e: Exception) {
            Result.failure(e)
        }

         */
    }
}

 */